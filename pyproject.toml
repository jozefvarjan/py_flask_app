[project]
name = "py-flask-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "poethepoet>=0.47.1",
]

[tool.poe.tasks]
dev = "flask --app main run --debug --port 5000"
