[project]
name = "py-flask-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-smorest>=0.47.0",
    "poethepoet>=0.47.1",
    "python-dotenv>=1.2.2",
]

[tool.poe.tasks]
dev = "flask --app main run --debug --port 5000"
prod = "flask run"
